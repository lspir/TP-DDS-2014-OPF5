package opf5;

import static org.junit.Assert.*;

import org.junit.Test;

public class TestDriver {/*

	@Test
	public void test() {
		Condicion condicion = partido -> partido.inscripciones().size() == 9;
	}

	@Test
	public void comoSeVeriaConObservadores() throws Exception {
		Partido partido = new Partido();
		Observador notificarAdmin = new ObservadorNotificarAdmin("toto@lalala.com");
		partido.cuandoTeLlenes(notificarAdmin);
		
		partido.agregarJugador(unJugador);
		//observadores.forEach(observador -> observador.notificarPartidoLleno())
		//observadores.forEach(observador -> observador.notificarJugadorInscripto(jugador))
		
		
	}
	
	class ObservadorJugadorAgregado implements Observador {
		
		public void notificarJugadorInscripto(Jugador jugador) {
			jugador.amigos().forEach(amigo -> mailSender.notificar(amigo.dfdd));
		}
	}*/
}

